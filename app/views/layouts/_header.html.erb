<% if logged_in?%>
    <nav class="container-fluid bg-dark p-4">
    <div class="row">
        <div class = "col col-3 d-flex text-light fs-3">
            Reserva de sala
        </div>
                <div class=" col col-6 d-flex justify-content-around text-light fs-4">
                    Olá <%= current_user.name %>
                </div>
            
                <div class = "col col-3 d-flex justify-content-end">
                    <a class="btn btn-md btn-outline-light " data-toggle="collapse" href="/sign_out" role="button" aria-expanded="false" aria-controls="collapseExample">Sair </a>
                </div>        
    </div>
    </nav>
<%else%>
teste
<%end%>

